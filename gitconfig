# This is Git's per-user configuration file.
[user]
	email = yuval@portrait-analytics.com
	name = Yuval Merhav
# Please adapt and uncomment the following lines:
#	name = Yuval Merhav
#	email = yuval@portrait-analytics.com
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[pull]
	rebase = true
